# 圍棋（`pg-weiqi`）— 遊戲規劃文檔

> **用途：** 本 repo 的遊戲權威規格——coding agent 改動前必讀：這個遊戲是什麼、規則、設計限制、優化方向。
> **整理方式：** 從本 repo 實作反向整理（2026-08-23）。**改玩法先改此檔再改碼**；本檔與程式碼衝突時，以「規則（§3）」描述的設計意圖為準回報差異。
> **上游契約：** [PG-GAME-AGENT-GUIDE.md](https://github.com/sampot/playgrounds/blob/main/docs/PG-GAME-AGENT-GUIDE.md)（唯一必讀；本檔不重複其全文）· 型錄條目 `playgrounds/catalog/entries/pg-weiqi.yaml`

## 1. 一句話

9×9 首刀圍棋：落子、氣、提子、禁著（自殺禁＋全盤禁重複）、虛手／數子終局（黑子＋空 vs 白子＋空＋貼 5.5），熱座雙人或對「吃子優先」隨機 AI；致敬通用圍棋規則的簡化實作，非任一競技規程復刻。

## 2. 定案速覽（待上架驗收）

| 項 | 值 |
| --- | --- |
| catalog id / kind / series | `pg-weiqi` / `game` / `桌遊` |
| status | `unlisted`——**待上架驗收** |
| 模式 | 熱座雙人（pvp）／對 AI；單局制 |
| 棋盤/勝負 | 9×9（`N=9`）；數子法，白貼 **5.5**（`KOMI`） |
| 對手 AI | 隨機合法著手、最大提子優先（無難度分級） |
| 素材 | 全程式繪製（木紋盤／棋子）＋WebAudio 合成音效（master 0.2） |
| 持久化 | 最佳提子數 `pg-weiqi-best`（KV 權威＋localStorage 快取）；音效開關僅記憶體 |
| 交付形 | ESM 純 JS＋Canvas；無 build；`npx --yes vitest@latest run` 測試 |

## 3. 完整規則（現行實作）

### 3.1 落子、氣與提子（`game.js` 純函式）

- `board[y][x]`，0 空／1 黑／2 白；黑先白後輪流。
- `groupAt` 洪水填充算連通組（同色相鄰合併）與氣（四鄰空點集合）；組內任一點無氣即整組可提。
- `play(x,y)`：落子後 `removeDeadGroups` 移除盤上所有零氣組；只把**對方色**的被提子計入 `captures[turn]`。落子前已先在試算盤驗證過自殺禁，故己方新組必有氣。

### 3.2 禁著判定（`isLegal`）

- 直接拒絕：越界、終局後落子、已有子的交叉點。
- **自殺禁**：複製盤面落子→提完對方死子後，若自己所在組零氣 → `reason:"suicide"`（先提後驗，故「撲入提劫形」等合法犧牲手正確放行）。
- **打劫／禁重複**：試算盤面序列化（`boardKey`，逐列 join 以 `/` 相連）若出現在 `history`（含初始空盤）→ `reason:"ko"`。實作語義＝**全盤禁重複局面（positional superko）**，比傳統「一手劫禁」更嚴：任何回到歷史上出現過局面的著手都禁。劫材在別處落子改變盤面後即可提回。

### 3.3 終局與數子（`pass`／`score`）

- 虛手：`passCount+1` 並換手；**連續兩手虛手**自動終局。也可直接按「數子終局」立即 `finish()`。
- 數子（台灣／中國簡化）：複製盤面 → 清零氣組 → 找連通空區域；空區域**僅接觸單一顏色**則整區計該色地盤，雙色交界為中性不计分。
- 得分＝活子數＋單色包圍的空點數；白總分另加 KOMI **5.5**；總分高者勝（5.5 為半數，實務上無和棋，程式仍保留 null 分支）。結果字串「黑勝／白勝」附雙方總分。

### 3.4 AI 行為（`aiMove`）

- 列舉全盤所有合法點並帶 `capturedCount`；取提子數最大的一群隨機挑一（有吃必吃）；無提子時在全部合法點中**均勻隨機**。無任何合法著手回 null，app 層代為虛手。
- 出手節奏：rAF 迴圈計時器 0.45s 後應手；AI 不會主動動虛手（僅無路可走時）。
- 已知弱點（如實記載）：不會逃單子、不自救氣緊組、不懂做眼——強度約「會提子的隨機」。

### 3.5 邊界處理

- 打劫提示文案：「打劫：不得立即提回同一局面」「禁著：不得自殺落子」寫入結果列（不用原生對話框）；佔位點靜默忽略不發音。
- 「切換 雙人/AI」按鈕在對局進行中鎖定（`btnMode.disabled`）；「新局」保留目前模式重開。
- AudioContext 需手勢解鎖：首次 `pointerdown`（body once）＋每次操作前 `unlock()`。

## 4. 操作與畫面

| 輸入 | 動作 |
| --- | --- |
| 點棋盤交叉點 | 落子（vs-AI 模式僅黑方回合可點） |
| 虛手 | 放棄一手；雙連虛手即數子終局 |
| 數子終局 | 立即結算 |
| 切換 雙人/AI | 僅對局外可切 |
| 新局 | 重開（非破壞性，不需確認） |
| 音效開/關 | 靜音（僅記憶體） |

- Canvas 360×360（PAD 20、CELL 40）：程序木紋底＋棋盤線＋星位 (2,6)²（**不含天元**）；棋子徑向漸層＋陰影＋高光；最後一手反色細環標記。
- HUD：輪到誰、雙方提子數、模式、最佳提子數、結果列。Mobile-first 單欄；禁原生對話框。

## 5. 持久化（KV 權威）

| key | 內容 | 讀寫時機 |
| --- | --- | --- |
| `pg-weiqi-best`（KV，權威） | 歷史最高「雙方合計提子數」（整數字串） | 提子或終局時超過才 PUT；啟動時 GET（`/^\d+$/` 才採用）。裸 `fetch('/api/kv/…')` 直呼，未走 `window.PG.kv` |
| `pg-weiqi-best`（localStorage，快取） | 同值 | 啟動立即讀顯示；寫入同步跟 KV 並行 |

- 注意：此 key 語義是「本局雙方總提子數峰值」，不是玩家成績——pvp 也會刷新，且黑白提子混計。改語義須換新 key 遷移。
- 多人協定小節：**未實作（無 Invite/session 支援）**。型錄條目亦無 protocols；`functions.js` 僅 stub（回 `{ok,name,path}`）。未來接入請仿 `pg-gomoku` 的 `protocol.js`＋functions 權威模式。

## 6. 美術／音效／署名

- 無外部素材（repo 無 `assets/` 目錄）：木紋盤、黑白棋子、星位全 Canvas 程序繪製（`app.js`）；詳 `ATTRIBUTION.md`。
- 音效全 WebAudio 合成（`audio.js`，master 0.2）：落子三角波 180Hz＋正弦 90Hz 木質聲、提子雙上行音、虛手短音、禁著低鳴、開始兩音、終局三音琶音。無第三方取樣。
- 新增素材：拷進 `assets/`、更新 `ATTRIBUTION.md`（CC0 也署名）、同步 `sam-manifest.json` files。

## 7. 測試（`npx vitest run`，僅 `game.test.js`）

現有覆蓋（15 例）：空盤建立與中心空點；單子四氣（`groupAt`/`hasLiberties`）；四面圍死整組移除；`play()` 提子並計入 captures；拒絕佔位與越界；角上自殺禁（`reason:"suicide"`）；完整劫例——提劫後對手立即提回被禁、別處下劫材後提回合法；空盤白靠貼目勝；黑地計入得分；數子前清死子（角上零氣白子不計分）；AI 空盤返回合法點；AI 三面包圍下必選提子點；落子交替換手；兩次虛手終局且產生勝者。

缺口：superko「非劫的重複局面」案例、中性空區（雙色交界）不計分、「尚有一口氣的死子不會被清」行為、AI 無合法手代虛手路徑皆未測。改規則必補。

## 8. 硬約束（不可違反）

1. 僅 HTML＋CSS＋JS（ESM）；**無 build**、不入庫 `node_modules`、不安套件；工具一律 `npx <pkg>` 臨時執行。
2. 禁瀏覽器原生 `alert`／`confirm`／`prompt`；禁著與錯誤一律結果列文字＋音效。
3. Mobile-first；主操作不可 hover-only。
4. 分數唯一權威＝`/api/kv/pg-weiqi-best`；localStorage 只能當啟動快取；新增任何統計一律走 `/api/kv/{key}`。
5. 規則核心維持在 `game.js` 純函式（DOM-free、可直接單元測試）；app.js 只做繪製與時序。
6. 不自行載入 `sdk.js`；本作目前不依賴 `window.PG`，靜態伺服即可玩。
7. 改動可執行邏輯前先寫失敗測試（TDD）。
8. 檔案清單變動須同步 `sam-manifest.json`。

## 9. 優化建議（可玩性與樂趣）

依優先級；實作前先在此登記並補測試。原則：補齊「首刀入門」體驗，不改變 9×9 數子制認同。

**高優先**

1. **上架驗收衝刺**：status 仍 unlisted。補 §7 缺口測試、確認 thumbnail/manifest 一致、實機走訪 go/play 兩殼後改 listed——文檔與型錄同步是驗收前置。
2. **死子人工標記再數子**：現行只清零氣組，「還有一口氣的死棋」全被當活子計。終局進入標記模式讓雙方點選死子群再結算——沒有它，數子結果常與常識不符，是新手最大困惑源。
3. **AI 強化一級**：加「逃氣緊己組」「不填自己眼」「貼著對方斷點長」三條啟發式評分（仍隨機擾動），成本極低但從「會提子的隨機」升到「像樣的陪練」。

**中優先**

4. **形勢視覺化**：落子後以半透明色塊粗估單色包圍空區（沿用 `findAreas`），讓新手看懂「圍地」概念。
5. **悔棋一手**：保存 history 快照堆疊，撤銷上一手（含提子復原）；熱座教學情境剛需。
6. **讓子／貼目選項**：讓 2–5 子或改 KOMI（0.5/5.5/7.5），平衡親子與新手對局；score() 已參數化，只需面板與存檔欄位。
7. **最佳值語義修正**：`pg-weiqi-best` 改記「你（黑）淨勝目數」或勝場，舊 key 棄用——現行雙方混計提子幾乎無成就感可言。

**低優先**

8. **棋譜與覆盤**：記錄手序（含提子），終局提供逐步回放；音效開關持久化到 localStorage 順手一起做。
